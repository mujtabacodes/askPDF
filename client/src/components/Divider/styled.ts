import styled from '@emotion/styled'
export const Container = styled.div`
	display: flex;
	align-items: center;
	width: fit-content;
	width: 100%;
	gap: 1rem;
	margin: 0 auto;
	padding: 1rem 5rem;
	div {
		width: 100%;
		height: 0.1rem;
		background: rgba(0, 0, 0, 0.195);
	}
	span {
		color: black;
	}
`
